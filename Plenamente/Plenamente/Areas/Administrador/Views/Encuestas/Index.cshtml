@model IEnumerable<Plenamente.Models.Encuesta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<div class="colorVerdeBack">
    <p>Encuestas</p>
</div>

<div class="contenedorBotones">
    <form asp-action="Index" method="get">
        <input type="text" id="buscarDato" name="searchString" value="@ViewData["CurrentFilter"]" />
        <input type="submit" value="Buscar" class="btn btn-success tamañoBoton" />
        <a href="Index" class="ayudaTitulos"><span>Ver todos</span><img src="~/Imagenes/binoculares.png" /><p>Ver</p></a>
        <a href="Create" class="ayudaTitulos"><span>Crear cargo individual</span><img src="~/Imagenes/archivo.png" /><p>Crear</p></a>
    </form>
</div>
<table id="tablaVista" class="tablaVista">
    <thead class="cabeceraTabla">
        <tr>
            <th class="colorVerde">
               Empresa
            </th>
            <th class="colorVerde">
                Fecha Vencimiento
            </th>
            <th class="colorVerde">
               Estado 
            </th>
            
            <th class="colorVerde"></th>
        </tr>
    </thead>
    <tbody class="cuerpoTabla">
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Empresa.Empr_Nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Encu_Vence)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Encu_Estado)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Encu_Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Encu_Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Encu_Id })
                </td>
            </tr>
        }
    </tbody>
</table>